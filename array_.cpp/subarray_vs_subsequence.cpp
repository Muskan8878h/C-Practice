// SUBARRAY - continuous part of the array.
// number of subarray of an array with the element = nC2 + n = (n*(n+1))/2

// SUBSEQUENCE - is a sequence thatcan be derived an array by electing zero or more elements, without changing the order of thr remaining elements.
// number of subsequence of an array with the element = 2^n

// everysubarray is a subsequence but every subsequence is not a subarray.
