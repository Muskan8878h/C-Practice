// in map only unique keys exist 
// but in multimap more than one key can exist 

#include<bits/stdc++.h>
using namespace std;
int main(){
    multimap<int,int> m;
    // multimap are rarely used insted of multimap we use as following
    map<int,vector<string>> m;
}